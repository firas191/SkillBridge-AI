# ---- Web framework ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# ---- Settings / validation ----
pydantic==2.10.4
pydantic-settings==2.7.1

# ---- Persistence ----
SQLAlchemy==2.0.36

# ---- LLM orchestration ----
# Ranges (not hard pins) so the resolver can pick a mutually-compatible set
# across the langchain ecosystem.
langchain>=0.3.0,<0.4
langchain-core>=0.3.0,<0.4
# NVIDIA NIM (default provider). OpenAI-compatible client used for Ollama / other endpoints.
langchain-nvidia-ai-endpoints>=0.3.0,<0.4
langchain-openai>=0.2.0,<0.4

# ---- Skill normalization ----
rapidfuzz==3.11.0
numpy==2.2.1

# ---- Document parsing (real CV files) ----
pdfplumber==0.11.4
python-docx==1.1.2

# ---- Offline OCR (images + scanned PDFs; no API key, no native binary) ----
# RapidOCR runs PaddleOCR's detection/recognition models via ONNX Runtime.
rapidocr-onnxruntime==1.4.4
PyMuPDF==1.25.1
Pillow==11.1.0

# ---- Utilities ----
httpx==0.28.1
tenacity==9.0.0

# ---- Optional local embeddings (uncomment to enable semantic skill matching offline) ----
# sentence-transformers==3.3.1

# ---- Dev / test ----
pytest==8.3.4
pytest-asyncio==0.25.2
